node_modules/
dist/
build/
.next/
.vite/
coverage/
.state/trace.json
.state/gate.json

# TypeScript incremental build cache（按机器生成；本机 dev 时偶尔需要 rm）
tsconfig.tsbuildinfo

# 本地环境变量（含凭据，不进 git）
.env
.env.local
.env.*.local
!.env.example
!.env.test

# Claude 临时文件
.claude/
.superpowers/

# .env.production: 部署契约真值(含 secret),gitignored 不进 git(2026-08-28 起,同 .env.local 模式)
# key 集合由 suite L0.5 门核对(存在时与 .env.example 严格双向比对);真值经 scripts/sync-secrets 进 GitHub Secrets
.env.production
