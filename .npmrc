# file: 依赖安装策略：拷贝而不是 symlink。
#
# 原因：npm 默认对 "file:../saas-identity-platform-msw" 建 symlink，Windows 上
# 会写成 Git Bash 风格的绝对路径 `/d/zcqiand-life/...`，原生 Node / Next.js 跑在
# Windows 不识别这条路径 → 解析失败：
#   "Module not found: Can't resolve '@saas/identity-platform-msw'"
#
# 副作用：msw 仓源码改动不会即时反映到此仓 node_modules，必须重新 `npm install`
# 拉一次新副本。
#
# 别删除这行 —— 删了就回到 symlink 模式、Next.js 起不来。
install-links=true