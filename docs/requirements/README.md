# requirements/

需求文档（REQ）。每条需求一篇，按编号递增。

**与 REF `output/saas-identity-platform/docs/requirements/` 对齐**：同样的需求颗粒度（影响功能子项 + 验收标准）；差异在实现栈（Next.js ↔ React+Vite）。

模板见 `_TEMPLATE.md`（按需从 REF 同步）。

REF 现有内容（按需从 REF 同步到本目录）：

- `REQ-2026-001.md` —— 多租户管理需求
- `REQ-2026-002.md` —— 用户与组织管理需求
- ...

nextjs 仓 REQ：

- `REQ-2026-006.md` —— 5 个管理页租户树过滤（5 个新增 fn-ID 待 tree-change 批准）
